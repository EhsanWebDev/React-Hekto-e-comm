

const Button = ({ title = "Shop Now" }) => {
    return (
        <button className="bg-pink-600 w-full text-white text-sm font-jose rounded px-6 py-2 md:w-auto">{title}</button>
    )
}

export default Button
